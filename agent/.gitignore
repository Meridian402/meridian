.env
.env.*
!.env.example
node_modules/
dist/
position-state.json
.vercel

# session-key spike state (deployed addresses, no secrets)
_sk-state.json
merd-daily-plan.jsonl
merd-daily-done.jsonl

# Runtime lock held by the 2-minute engage job; per-machine, never shared.
merd-engage.lock
