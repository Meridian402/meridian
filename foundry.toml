# Foundry config for the Solidity in contracts/. The repo is TypeScript-first —
# these are the only on-chain pieces we author ourselves — so src points at
# contracts/ rather than the default src/, which holds the agent backend.
[profile.default]
src = "contracts"
out = "contracts/out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
# Robinhood Chain is an Arbitrum Orbit L2; London is the safe floor for the
# opcodes these contracts use.
evm_version = "london"
